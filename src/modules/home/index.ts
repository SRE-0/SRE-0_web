/**
 * Home module public API.
 * Only what is exported here can be imported by other parts of the app.
 * Internals (containers, hooks, services) stay private to this module.
 */
export { default as HomePage } from "./pages/Home";