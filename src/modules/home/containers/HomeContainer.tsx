import React from 'react';
import HomeView from '../components/home_view/HomeView';

export const HomeContainer = () => {
  //const { uiState, refresh } = useHomeData();

  return <HomeView 
    //cards={uiState.cards} stats={uiState.stats} onRefresh={refresh} 
  />;
};